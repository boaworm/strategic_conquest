{
  "name": "@sc/trainer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node --enable-source-maps dist/index.js",
    "train": "node src/index.ts",
    "collect": "npx tsc -p ../shared/tsconfig.json && npm run build && node src/collect_data.ts",
    "collect-moe": "npx tsc -p ../shared/tsconfig.json && npm run build && node src/collect_moe_data.ts",
    "nn-sim": "node src/nn_simulator.ts",
    "record": "npx tsc -p ../shared/tsconfig.json && npm run build && node src/record_replay.ts",
    "replay": "cd ../client && npm run build 2>/dev/null; cd ../trainer && node src/replay_picker.ts",
    "test_replay": "cd ../client && npm run build 2>/dev/null; cd ../trainer && node src/test_replay_picker.ts",
    "profile": "node src/profile_game.ts",
    "profile:cpu": "node src/profile_game.ts --profile",
    "nn-evolve": "python packages/trainer/ai/evolve.py",
    "nn-server": "python ai/server.py"
  },
  "dependencies": {
    "@sc/shared": "*",
    "@sc/testing": "*",
    "onnxruntime-node": "1.24.3"
  }
}
